# macOS
.DS_Store

# Build artifacts
build/
install/
log/

# Docker/dev artifacts
*.log

# Keep workspace scripts/docs tracked, but not vendored upstream repo clone
# Cloned xarm_ros2 upstream is ignored, custom packages are tracked individually
src/xarm_ros2/.gitkeep
